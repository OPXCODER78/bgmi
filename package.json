{
  "name": "ai-companion-mobile",
  "version": "1.0.0",
  "description": "Mobile web AI companion (Gemini) with type-to-send chat",
  "main": "server.js",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "echo \"No build step required for this project\""
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}
